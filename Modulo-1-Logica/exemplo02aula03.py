palavra = 'sol'

# Estrutura de iteração precisa de duas coisas:
# 1. Variável auxiliar de acesso(acessa cada item da coleção);
# 2. coleção que será iterada(ou percorrida).

# Computador, utilize a variável de acesso para acessar cada elemento da coleção.
for letra in palavra:
    print(letra)